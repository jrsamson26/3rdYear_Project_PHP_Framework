{% extends '_base.html.twig' %}

{% block main %}

    <p>Welcome {{ username }}</p>

    <!--Killing the session-->
    <a href = "/killSession">Log Out</a>

    {% if message %}
        <p>{{ message }}</p>
    {% endif %}

    {% include 'footer.html.twig' %}
{% endblock %}