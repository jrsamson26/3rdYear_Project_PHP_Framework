{% extends '_base.html.twig' %}

{% block main %}
    <!--This is the log out the page-->
    <div id = "logout">
        <a href = "/killSession">Log Out</a>

        {% if message %}
            <p>{{ message }}</p>
        {% endif %}
    </div>

    <!--This is the delete the student-->
    <div id ="deleteStudent">

    <h1>Remove Students</h1>

    <form action="/processRemoveStudent" method="post">

        <span class = "form1">
        <label for = "username">username:</label>
            <input type="text" name="username"  placeholder="Enter username must filled in" autofocus />
        </span>

        <span class = "form2">
        <label for = "id">id:</label>
            <input type="text" name="id"  placeholder="Enter meeting ID must filled in" autofocus />
        </span>

        <span class = "form5">
            <input type="submit" value="Submit">
            <input type="reset" value="Reset">
        </span>
    </form>

    </div>

    <div id = "displayStudent">
        {% include 'listStudents.html.twig' %}
    </div>
{% endblock %}